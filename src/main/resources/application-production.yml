app:
  dynamodb:
    use-default-credentials: true
  cache:
    type: redis
aws:
  dynamodb:
    createTablesOnStartup: false
spring:
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
